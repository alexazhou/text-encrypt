a text encrypt program use Python and pyqt，the core algorithm is 128bit DES

TextEncrypt is encrypt program
des_dll is encrypt algorithm core

python version need:

python3.2
PyQt4.9.2
PyWin32

But the exe version don't need any other environment

--------------------------------------------------------------

一个使用Python和PyQt编写的文本加密程序，核心算法为128位DES

TextEncrypt中是加密程序，包括图形界面和其他的一些控制
des_dll中是核心加密算法的实现

直接运行py版本需要先确保安装了以下部件:
python3.2
PyQt4.9.2
PyWin32

也可以直接下载我编译的exe版本，这个版本不依赖其他的东西
